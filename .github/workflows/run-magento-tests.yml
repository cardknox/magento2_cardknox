name: m2-actions-test
on: 
  push:
    branches-ignore: 
      - 'master'
      - 'rc/*/*/*'
      
jobs:
  magento2-build:
    runs-on: ubuntu-latest
    name: 'm2 unit tests & build'
    services:
      mysql:
        image: docker://mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: magento
          MYSQL_DATABASE: magento
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3
      #elasticsearch:
        #image: docker://elasticsearch:7.1.0
        #ports:
        #  - 9200:9200
        #options: -e="discovery.type=single-node" --health-cmd="curl http://localhost:9200/_cluster/health" --health-interval=10s --health-timeout=5s --health-retries=10
      opensearch:
        image: ghcr.io/mad-i-t/magento-opensearch:2.5.0
        ports:
          - 9200:9200
        options: -e="discovery.type=single-node" -e "plugins.security.disabled=true" -e "plugins.security.ssl.http.enabled=false" --health-cmd="curl http://localhost:9200/_cluster/health" --health-interval=10s --health-timeout=5s --health-retries=10
     
    steps:
    - uses: actions/checkout@v3

    - name: Setup PHP
      uses: shivammathur/setup-php@v2

    - name: 'Install Magento'
      uses: MAD-I-T/magento-actions@v3.20
      with:
        process: 'install-mage-os'
        magento_version: 2.4.6
        no_push: 1

    - name: Install dependencies
      run: composer install

    - name: Update Composer
      run: |
        cd magento
        sudo php bin/magento setup:install
        sudo composer update
      # sudo php bin/magento setup:install
      #  sudo php bin/magento setup:upgrade/setup:install

    - name: Run Unit Tests
      run: |
        pwd
        php vendor/phpunit/phpunit/phpunit -c vendor/phpunit/phpunit/phpunit.xml.dist Test
      #  sudo php bin/magento module:enable --all
      #  sudo php bin/magento setup:di:compile
      #  php ./vendor/phpunit/phpunit/phpunit -c ./vendor/phpunit/phpunit/phpunit.xml.dist ./Test

      # php magento setup:upgrade

      #  pwd
      #  ls
      #  echo CHANGING TO VENDOR
      #  cd vendor
      #  ls
      #  echo CHANGING TO PHPUNIT
      #  cd phpunit
      #  ls
      #  echo CHANGING TO PHPUNIT
      #  cd phpunit
      #  ls
      #  php vendor/phpunit/phpunit -c vendor/phpunit/phpunit/phpunit.xml.dist Test
      #  php bin/phpunit
      # -c dev/tests/unit/phpunit.xml.dist Test
        

    


        
    #- name: 'this step will build an magento artifact'
    #  uses: MAD-I-T/magento-actions@v3.20
    #  with:
    #    process: 'build'
        
    #- name: 'Run Magento unit tests'
    #  uses: MAD-I-T/magento-actions@v3.20
    #  with:
    #    process: 'unit-test'
    #    unit_test_config: 'dev/tests/unit/phpunit.xml.dist'
    #    unit_test_subset_path: '../Test/'
